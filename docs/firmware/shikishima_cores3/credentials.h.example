// SC Firmware Credentials Template
// Copy to credentials.h (gitignored) and fill in real values.
// NEVER commit credentials.h — it is in .gitignore.

#pragma once

// WiFi
#define SC_WIFI_SSID     "YOUR_ACTUAL_SSID"
#define SC_WIFI_PASSWORD "YOUR_ACTUAL_PASSWORD"

// Control token — must match STACKCHAN_CONTROL_TOKEN in .env.local
// Leave empty string "" to disable token auth (not recommended in production)
#define SC_CONTROL_TOKEN "YOUR_CONTROL_TOKEN"
