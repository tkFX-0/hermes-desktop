# SC-AI-01 Voice One-shot Evidence Template

result: PASS / PARTIAL / HOLD / STOP

```text
voice:
  route_selected:
  exact_text:
  spoken_once:
  unexpected_motion:
  mic_used:
  firmware_write:
  custom_firmware:
  evidence_photo_or_video:
  gate_restored_hold:

safety:
  motion_dance_used:
  camera_used:
  productionReady:
  execution:
  rawValuesReported:
```

Do not record raw device identifiers, tokens, secrets, or local-only values.
Gate must return to HOLD after the one-shot.

